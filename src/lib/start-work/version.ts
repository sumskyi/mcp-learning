// TODO: bump the version field in the repo's manifest (package.json, gemspec, etc.)
// and return the new version string.
export async function bumpVersion(repoPath: string): Promise<string> {
  throw new Error(`not implemented: bumpVersion(${repoPath})`);
}
